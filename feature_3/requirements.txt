# Runtime dependencies for the F3 Predictive Stability Sentinel baseline.
pandas>=2,<4
scikit-learn>=1.5,<2
joblib>=1.3,<2

# Optional candidate model (script falls back to sklearn GradientBoosting
# if unavailable — no prebuilt wheel for very new Python versions may force
# a slow source build).
xgboost>=2,<4
