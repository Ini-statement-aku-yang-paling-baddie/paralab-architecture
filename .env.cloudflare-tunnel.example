# Copy to .env.cloudflare-tunnel. This token authorizes the connector: never commit or share it.
# In Cloudflare Zero Trust: Networks > Tunnels > Create a tunnel > Docker.
TUNNEL_TOKEN=replace-with-the-token-generated-for-this-tunnel
